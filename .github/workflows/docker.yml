name: docker

# Builds and pushes the Docker image on a GitHub Release.
#   - every release  -> :<version>  (e.g. :0.1.0)
#   - normal release -> :latest
#   - pre-release    -> :beta
#
# Required repository secrets:
#   DOCKERHUB_USERNAME  Docker Hub account; also the image namespace (<user>/labeljetty)
#   DOCKERHUB_TOKEN     Docker Hub access token (Account Settings -> Security)
on:
  release:
    types: [published]

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compute version and tags
        id: meta
        env:
          TAG_NAME: ${{ github.event.release.tag_name }}
          PRERELEASE: ${{ github.event.release.prerelease }}
          IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/labeljetty
        run: |
          VERSION="${TAG_NAME#v}"
          TAGS="${IMAGE}:${VERSION}"
          if [ "${PRERELEASE}" = "true" ]; then
            TAGS="${TAGS},${IMAGE}:beta"
          else
            TAGS="${TAGS},${IMAGE}:latest"
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          build-args: |
            VERSION=${{ steps.meta.outputs.version }}
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
